"""Foundation for F3, F8, F9 and F10 — every photo and voice note.

OWNER: Shreekumar

Serves:
    POST /assets/presign

Specified by: docs/API_CONTRACT.md §3. Presigned uploads only: the API never receives raw bytes.

Structure only. No routes are defined yet and nothing is mounted in
app/main.py — an empty router module has nothing to mount.
"""
